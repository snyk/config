{
  "name": "snyk-config",
  "description": "Config setup for snyk shared across projects",
  "main": "lib/index.js",
  "scripts": {
    "test": "snyk test -q && node test/*.test.js --afoo --azoo='true' | tap-spec",
    "semantic-release": "semantic-release pre && npm publish && semantic-release post"
  },
  "keywords": [
    "config"
  ],
  "author": "Remy Sharp",
  "license": "UNLICENSED",
  "devDependencies": {
    "semantic-release": "^4.1.0",
    "snyk": "^0.3.2",
    "tap-spec": "^2.2.2",
    "tape": "^4.0.1"
  },
  "dependencies": {
    "debug": "^2.2.0",
    "nconf": "^0.7.2",
    "path-is-absolute": "^1.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Snyk/config.git"
  }
}
