{
  "name": "snyk-config",
  "description": "Config setup for snyk shared across projects",
  "main": "lib/index.js",
  "scripts": {
    "lint": "jscs lib/*.js -v",
    "test": "npm run lint && snyk test > /dev/null && tap test/*.test.js --cov",
    "semantic-release": "semantic-release pre && npm publish && semantic-release post"
  },
  "keywords": [
    "config"
  ],
  "author": "Remy Sharp",
  "license": "Apache-2.0",
  "devDependencies": {
    "jscs": "^2.8.0",
    "semantic-release": "^4.1.0",
    "snyk": "^1.1.0",
    "tap": "^5.0.1",
    "tape": "^4.0.1"
  },
  "dependencies": {
    "debug": "^2.2.0",
    "nconf": "^0.7.2",
    "path-is-absolute": "^1.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Snyk/config.git"
  }
}
